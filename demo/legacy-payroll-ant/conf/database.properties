# DELIBERATELY LEGACY. See ../README.md.
#
# ============================================================================
# EVERY CREDENTIAL IN THIS FILE IS FABRICATED. Invented for this fixture so the
# analyzer has plaintext credentials to detect and the Redactor has something to
# redact. Not real, not in use anywhere.
# ============================================================================

# Direct JDBC settings, used by the batch jobs. The web tier uses the container
# datasource instead, so these two drift apart and nobody notices until a release.
jdbc.driver=oracle.jdbc.driver.OracleDriver
jdbc.url=jdbc:oracle:thin:@payroll-prod.acme.internal:1521:PAYPROD
jdbc.username=payroll_batch
jdbc.password=Payr0ll2009

# The same credentials again, embedded in a URL, for the reporting tool.
reporting.datasource=jdbc:oracle:thin:payroll_ro/R3port1ng@payroll-prod.acme.internal:1521:PAYPROD

# Connection pool sized in 2009 for a server with 2 GB of RAM.
jdbc.pool.initialSize=50
jdbc.pool.maxActive=50
jdbc.pool.removeAbandoned=true
jdbc.pool.removeAbandonedTimeout=300

# Hardcoded absolute paths on the one server this runs on.
payslip.output.dir=/opt/acme/payroll/payslips
payslip.archive.dir=/mnt/nas01/payroll-archive
batch.lock.file=/var/run/acme/payroll.lock
report.template.dir=C:\\AcmePayroll\\templates

# SMTP for payslip delivery.
mail.smtp.host=smtp.acme.internal
mail.smtp.username=payroll-noreply
mail.smtp.password=sendmail2011

# Third-party bank file transfer.
bank.sftp.host=sftp.examplebank.test
bank.sftp.username=acme_payroll
bank.sftp.privatekey=/opt/acme/payroll/keys/bank_rsa
bank.api.token=Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.fabricated.notarealtoken

# Feature toggles, changed by editing this file and restarting the server.
feature.newTaxBands=false
feature.selfServicePayslips=false
